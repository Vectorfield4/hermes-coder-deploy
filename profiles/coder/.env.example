# GitHub
GITHUB_TOKEN=your_github_pat_here

# Vercel (used by project-init to link new projects for staging deploys)
# VERCEL_TOKEN is required to link. VERCEL_ORG_ID / VERCEL_PROJECT_ID are optional:
# if set, project-init writes .vercel/project.json directly (see project-init skill).
VERCEL_TOKEN=your_vercel_token_here
VERCEL_ORG_ID=your_vercel_org_id_here
VERCEL_PROJECT_ID=your_vercel_project_id_here

# AI (DeepSeek)
OPENAI_API_KEY=your_deepseek_api_key_here
OPENAI_API_BASE=https://api.deepseek.com/v1

# Dense-Mem (RAG memory layer)
# Profile API key created via scripts/memory-bootstrap.sh (control portal).
DENSE_MEM_API_KEY=your_dense_mem_profile_api_key_here